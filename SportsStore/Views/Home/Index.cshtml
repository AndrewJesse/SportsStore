@model ProductsListViewModel

@{
}
@foreach (var p in Model?.Products ?? Enumerable.Empty<Product>())
{
    <div>
        <h3>@p.Name</h3>
        @p.Description
        <h4>@p.Price.ToString("c")</h4>
    </div>
}
<div page-page-model="@Model?.PagingInfo" page-action="Index"></div>
